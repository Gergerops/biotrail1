---
import { getDict, type Locale } from '~/i18n';

interface Props {
  locale: Locale | string;
}

const { locale } = Astro.props;
const dict = getDict(locale);
---

<header class="hero">
  <div class="container">
    <div class="hero-grid">
      <div class="hero-copy reveal">
        <span class="eyebrow">{dict.hero.eyebrow}</span>
        <h1 set:html={dict.hero.title}></h1>
        <p class="hero-sub">{dict.hero.subtitle}</p>
        <div class="hero-cta">
          <button class="btn btn-primary btn-arrow" data-open-modal data-plan="hero">
            {dict.hero.cta_primary}
          </button>
          <a class="btn btn-ghost" href="#how">{dict.hero.cta_secondary}</a>
        </div>
        <div class="hero-trust">
          <span>{dict.hero.trust_1}</span>
          <span>{dict.hero.trust_2}</span>
          <span>{dict.hero.trust_3}</span>
        </div>
        <div class="hero-quote">
          <div class="hero-quote-avatar" style={`background: ${dict.hero.quote_color};`}>{dict.hero.quote_initial}</div>
          <div class="hero-quote-content">
            <p class="hero-quote-text">"{dict.hero.quote_text}"</p>
            <div class="hero-quote-author">
              <span class="hero-quote-name">{dict.hero.quote_name}</span>
              <span class="hero-quote-meta">{dict.hero.quote_meta}</span>
            </div>
          </div>
        </div>
      </div>

      <div class="hero-visual reveal">
        <img
          src="/hero.png"
          alt="A doctor and patient in conversation in a sunlit Frankfurt clinic"
          loading="eager"
          width="800"
          height="1000"
        />
      </div>
    </div>
  </div>
</header>
